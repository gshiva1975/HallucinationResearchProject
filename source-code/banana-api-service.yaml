apiVersion: v1
kind: Service
metadata:
  name: banana-api-service
spec:
  selector:
    app: banana-api
  ports:
    - port: 80
      targetPort: 8000
      nodePort: 30080       # access via: minikube service banana-api-service
  type: NodePort
